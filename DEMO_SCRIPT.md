# DEMO_SCRIPT.md — תסריט לווידאו ההדגמה

**אורך יעד: 8 דקות.** הזמנים מצטברים. הטקסט המודגש הוא מה שאומרים; מתחתיו מה שרואים על המסך.

---

## הכנות לפני ההקלטה

| # | מה להכין | למה |
|---|---|---|
| 1 | **שני חשבונות** — שלך, ועוד אחד ("דנה כהן") עם לפחות אימון אחד | צריך אימון של מישהו אחר כדי להדגים לייק ותגובה |
| 2 | **4–6 אימונים בפיד**, לפחות אחד בלי תמונה ואחד עם כותרת עברית ארוכה | מראה שהמצבים הקיצוניים מטופלים |
| 3 | **קונסולת Firebase פתוחה** בלשונית `workouts`, בחלון נפרד | לקטע השרת החי |
| 4 | **הקלטת מסך של הטלפון** (`scrcpy` או Smart View) לצד הדפדפן | שניהם בפריים אחד בדקה 5 |
| 5 | **מצב תצוגה: כהה** בפתיחה | זה המראה שהמוקאפים מתארים |
| 6 | **מצב טיסה זמין בגישה מהירה** | לבאנר "אין חיבור" |
| 7 | Android Studio פתוח על עץ הפרויקט, מכווץ עד רמת החבילות | לקטע הארכיטקטורה |

> **טיפ:** אל תערוך את הפיד באמצע ההקלטה. הכל צריך להיראות כמו אפליקציה שכבר בשימוש.

---

## 0:00 – 0:30 · מה זו האפליקציה ולמי היא מיועדת

**על המסך:** מסך הפתיחה של האפליקציה, ואז גלילה איטית אחת בפיד. בלי הקלקות.

> "שלום, אני רועי, וזו FitShare — רשת חברתית קטנה בעברית לאנשים שמתאמנים.
>
> הרעיון התחיל מבעיה אמיתית: מי שמתאמן באופן קבוע מצלם את האימון ושולח לקבוצת וואטסאפ, ותוך יומיים
> התמונה נבלעת בין ההודעות. אין רשימה, אין חיפוש, ואין דרך לחזור לאימון שעשית לפני חודש.
>
> FitShare היא המקום שבו האימון הזה נשאר. מפרסמים אותו עם תמונה תוך פחות מדקה, כולם רואים אותו
> בפיד חי, אפשר לעשות לייק, להגיב, ולשמור למועדפים כדי לחזור אליו.
>
> הכל בעברית, הכל מימין לשמאל, ובעלות אפס — בלי כרטיס אשראי."

---

## 0:30 – 1:30 · ארכיטקטורה

**על המסך:** Android Studio, עץ הפרויקט. מרחיבים חבילה אחת בכל פעם בזמן שמדברים עליה.

> "לפני שנכנס למסכים, דקה על איך זה בנוי — כי כמעט כל החלטה כאן היא החלטה מודעת.
>
> **[מרחיבים `data/`]** ארבע שכבות. למטה `DataSource` — זו השכבה **היחידה** שיודעת ש‑Firebase
> קיימת. מעליה `Repository`, שהוא תמיד ממשק פלוס מימוש אחד. מעליו `ViewModel`, שתלוי בממשק ולא
> ב‑Firestore — ולכן אפשר לבדוק אותו בלי רשת. ולמעלה `Fragment`, שרק מנפח layout, מאזין ל‑LiveData
> ומצייר.
>
> **[מרחיבים `data/model/`]** המודלים כאן נקיים לגמרי מאנדרואיד. אין `R`, אין `@StringRes`, אין
> שום `import android`. ה‑enum של הקטגוריות הוא קבועים בלי ארגומנטים, והתרגום שלו לעברית קורה
> בפונקציה אחת בשכבת ה‑UI. אילו ה‑enum היה מחזיק `R.string`, שכבת המודל הייתה נקשרת לאנדרואיד.
>
> **[מרחיבים `di/`]** אין Hilt ואין Dagger. יש `ServiceLocator` אחד שכתוב ביד ו‑`ViewModelFactory`
> אחד. זו החלטה: מי שקורא את הקובץ הזה מלמעלה למטה יודע בדיוק מי בונה את מי.
>
> **[מרחיבים `ui/common/`]** וכאן החלק שאני הכי גאה בו — כל מה שחוזר יותר מפעם אחת גר כאן פעם
> אחת. `StateRenderer` הוא המקום היחיד שמחליט איזה מארבעת המצבים מוצג. `EnumLabels`, `TimeLabels`
> ו‑`NumberLabels` הם המקומות היחידים שהופכים ערך לטקסט. `MetaChips` הוא רכיב אחד — layout פלוס
> הקוד שממלא אותו — שגם כרטיס הפיד וגם מסך הפרטים משתמשים בו."

---

## 1:30 – 5:30 · מסך אחרי מסך

### 1:30 – 2:00 · התחברות ומצבי שגיאה

**על המסך:** מסך ההתחברות. מקלידים אימייל תקין וסיסמה **שגויה**. לוחצים התחברות.

> "מתחילים בהתחברות. שימו לב מה קורה כשהסיסמה שגויה — **[מופיע 'אימייל או סיסמה שגויים']** —
> הודעה בעברית שאומרת מה קרה. וזה לא במקרה: **כל** שגיאה באפליקציה עוברת דרך מחלקה אחת בשם
> `ErrorMapper`. שום Fragment לא בודק חריגות ושום טקסט שגיאה לא כתוב בשום מקום אחר.
>
> **[מקלידים סיסמה נכונה, נכנסים]** והחיבור נשמר — אם אסגור את האפליקציה עכשיו ואפתח מחדש, אני
> כבר בפנים."

### 2:00 – 2:50 · הפיד — ולמה הוא הלב של הפרויקט

**על המסך:** הפיד. גוללים. לוחצים על צ'יפ "כוח". מקלידים בחיפוש. מחליפים מיון ל"הכי אהובים".

> "זה הפיד, והוא **חי** — הוא לא נטען פעם אחת, הוא מאזין ל‑Firestore. עוד רגע נראה את זה בפעולה.
>
> **[לוחצים על 'כוח']** סינון לפי קטגוריה. השאילתה הזו — סינון פלוס מיון — דורשת אינדקס מורכב
> ב‑Firestore, והוא מתועד ב‑`firestore.indexes.json` שברפוזיטורי.
>
> **[מקלידים בחיפוש]** חיפוש לפי שם אימון או שם מתאמן. החיפוש רץ בצד הלקוח, וזו הסיבה שוויתרתי
> על עימוד: Firestore לא יודעת לחפש תת‑מחרוזת, אז אם הייתי טוען עשרים אימונים בכל פעם, המשתמש היה
> מחפש אימון שהוא **יודע** שקיים ולא מוצא אותו. עדיף פיד מלא וחיפוש שעובד.
>
> **[מיון להכי אהובים]** ומיון.
>
> **[מנקים סינון וגוללים לאימון בלי תמונה]** ושימו לב לאימון הזה — אין לו תמונה, והכרטיס עדיין
> נראה נכון. כל מסך באפליקציה מטפל בארבעה מצבים: טעינה, ריק, שגיאה ותוכן."

### 2:50 – 3:40 · פרסום אימון

**על המסך:** לוחצים על טאב "הוספה". בוחרים תמונה מהגלריה. ממלאים שם, תיאור, משך, קטגוריה, קושי. מפרסמים.

> "מפרסמים אימון. **[בוחרים תמונה]** התמונה נדחסת לפני ההעלאה — הצלע הארוכה נחתכת ל‑1080 פיקסלים
> ואיכות JPEG 80. מדדתי: תמונה של 223 קילובייט יורדת ל‑120. אף פעם לא מעלים קובץ גולמי מהמצלמה.
>
> **[ממלאים את הטופס]** ולידציה על כל שדה, בעברית.
>
> **[מפרסמים]** ושתי הפעולות — מסמך האימון והגדלת מונה האימונים של המשתמש — קורות ב‑`WriteBatch`
> **אחד**. או ששתיהן נכתבות, או שאף אחת. אם ההעלאה נכשלת באמצע, הטופס נשאר מלא ולא נכתב כלום.
>
> **[חוזרים לפיד — האימון שם למעלה]** והנה הוא, בלי לרענן."

### 3:40 – 4:40 · פרטי אימון — הלייק, התגובות, המועדפים

**על המסך:** נכנסים לאימון של **דנה** (לא שלך). לוחצים לייק. כותבים תגובה. שומרים למועדפים.

> "נכנסים לאימון. **[לוחצים על הלייק]** זה האלמנט החתימה של האפליקציה — סימן הלב‑ומשקולת מהלוגו,
> עם קפיצה קצרה והמונה שמתחלף בדעיכה. הוא נראה קטן, אבל טכנית הוא הדבר המעניין ביותר כאן: הלייק
> כותב מסמך ב‑`workouts/{id}/likes/{uid}` **ומגדיל את המונה באותה `runTransaction`**. שתי כתיבות
> נפרדות היו יכולות להשאיר מונה שקרי אם השנייה נכשלת. בדקתי את זה עם שתים‑עשרה הקשות מהירות —
> מספר מסמכי הלייק, המונה במסמך, והמספר על המסך היו זהים.
>
> **[כותבים תגובה]** תגובות — אותו עיקרון בדיוק, אותה טרנזקציה.
>
> **[לוחצים על הסימנייה]** ושמירה למועדפים. שימו לב שהלייק והשמירה הם **שני סמלים שונים** — לב
> ומשקולת מול סימנייה. בגרסה קודמת שניהם היו לב, וזה נראה כמו אותה פעולה פעמיים.
>
> **[לוחצים על שם הכותב]** ומכאן אפשר להיכנס לפרופיל של דנה, לקריאה בלבד."

### 4:40 – 5:30 · פרופיל, מועדפים, ערכת נושא, ואופליין

**על המסך:** טאב פרופיל. מצביעים על הסטטיסטיקות. מחליפים תצוגה לבהיר וחזרה. טאב מועדפים. מפעילים מצב טיסה.

> "הפרופיל שלי. שלוש סטטיסטיקות: אימונים, לייקים שקיבלתי, ומועדפים. **[מצביעים על 'לייקים']**
> 'לייקים' זה לייקים שקיבלתי — סכום הלייקים על האימונים שלי. והם עולים **אפס** קריאות נוספות
> ל‑Firestore, כי האימונים האלה כבר נטענו כדי לצייר את הגריד.
>
> חשוב לדעת: יש כאן **מסך פרופיל אחד**, לא שניים. הוא מקבל `userId` שיכול להיות `null` — `null`
> זה 'הפרופיל שלי' עם עריכה, מתג תצוגה והתנתקות, וערך אמיתי זה פרופיל של מישהו אחר לקריאה בלבד.
> שני מסכים כמעט זהים זו בדיוק הכפילות שרציתי להימנע ממנה.
>
> **[מחליפים למצב בהיר, ואז חזרה לכהה]** מצב בהיר וכהה, נשמר בין הפעלות.
>
> **[טאב מועדפים]** והמועדפים. השורה הזו נטענת **בשאילתה אחת**, כי מועדף נשמר כתצלום מפורק של
> האימון — כותרת, תמונה, שם הכותב — ולא כמצביע.
>
> **[מפעילים מצב טיסה]** ואם אין רשת — **[מופיע הבאנר האדום]** — יש באנר, והוא יושב על ה‑Activity
> ולא על מסך מסוים, כדי שאי אפשר יהיה לשכוח אותו במסך חדש."

---

## 5:30 – 6:30 · צד השרת, חי

**על המסך:** מסך מפוצל — הטלפון מימין, קונסולת Firebase משמאל, בלשונית `workouts`.

> "זה החלק שהכי כיף להראות. משמאל קונסולת Firebase, מימין האפליקציה **רצה, ואני לא נוגע בה**.
>
> **[בקונסולה: פותחים מסמך אימון ומשנים את `title`, שומרים]**
>
> **[מצביעים על הטלפון]** — הכותרת בפיד השתנתה. לא ריעננתי, לא נגעתי במסך. זה
> `snapshotListener` של Firestore עטוף ב‑`callbackFlow`, שמגיע ל‑ViewModel כ‑`Flow`, מתחבר שם עם
> הסינון והחיפוש, ומסתיים ב‑`asLiveData()`. וזה `DiffUtil` שמצייר מחדש **רק** את הכרטיס שהשתנה,
> ולא את כל הרשימה.
>
> **[בקונסולה: פותחים `workouts/{id}/likes` ומוסיפים מסמך ידנית, ומגדילים `likesCount` ב‑1]**
>
> **[מצביעים על הטלפון]** — והמונה זז. מישהו אחר עשה לייק, ואני רואה את זה מיד.
>
> **[עוברים ללשונית Rules]** ולסיום — חוקי האבטחה. משתמש לא מחובר לא קורא כלום. אתה יכול לערוך
> ולמחוק **רק** את האימונים שלך. אתה כן יכול לשנות `likesCount` ו‑`commentsCount` על אימון של
> מישהו אחר — כי בשביל לעשות לייק חייבים. **[מצביעים על שורת `hasOnly`]** והחוק הזה מגביל בדיוק
> אילו שדות מותר לגעת בהם. עוד רגע אחזור לזה, כי יש כאן מגבלה אמיתית."

---

## 6:30 – 7:15 · ספריות ולמה כל אחת

**על המסך:** `gradle/libs.versions.toml`, גלילה איטית.

> "בקצרה על הספריות, כי לכל אחת יש סיבה.
>
> **Firebase Auth ו‑Firestore** — שני רכיבי השרת. חינם, בלי הגבלה, ועם מאזינים בזמן אמת.
>
> **coroutines‑play‑services** — נותן `await()`, שהופך `Task` של Firebase לקריאת `suspend`.
> בלעדיו כל הקוד היה callbacks מקוננים.
>
> **Navigation Component עם SafeArgs** — Activity אחד, גרף אחד, וארגומנטים **מוטיפסים**. בלי
> SafeArgs ארגומנט נוסע כמחרוזת ב‑Bundle, וזה מקור קלאסי לקריסות.
>
> **Lifecycle — ViewModel ו‑LiveData** — `viewModelScope` מבטל קורוטינות לבד כשה‑ViewModel מת. זו
> ההגנה המרכזית מקריסה בסיבוב מסך.
>
> **Material Components** — כל רכיבי ה‑UI וערכת Material3 DayNight, שהיא מה שנותן מצב כהה ובהיר
> כמעט בחינם.
>
> **ConstraintLayout** — קריטי דווקא בגלל RTL: `constraintStart` ו‑`constraintEnd` מתהפכים לבד,
> ולכן אף מסך לא צריך גרסה נפרדת לעברית.
>
> **Glide** — טעינת תמונות עם מטמון ומודעות למחזור החיים. טעינה ידנית ל‑`ImageView` פירושה דליפות
> ותקיעות ב‑main thread.
>
> **Retrofit ו‑OkHttp** — הקליינט של Cloudinary כממשק מוטיפס.
>
> **ExifInterface** — קורא את תג הסיבוב של המצלמה. בלעדיו תמונה מצולמת עולה מסובבת ב‑90 מעלות.
>
> ומה שאין: אין Hilt, אין Compose, אין Room. **[עוצרים רגע]** ובמיוחד — **אין Firebase Storage**.
> וזה מוביל אותי לחלק האחרון."

---

## 7:15 – 8:15 · קשיים ומגבלות ידועות

**על המסך:** `README.md` פתוח בסעיף המגבלות, ואז חוזרים לאפליקציה לסיום.

> "ארבעה דברים שעלו בדרך ושווה לספר עליהם.
>
> **אחד — Firebase Storage לא בשימוש, וזו לא הזנחה.** Storage דורש היום תוכנית בתשלום, ובתוכנית
> החינמית כל קריאה נכשלת. לכן התמונות עוברות ל‑Cloudinary. אבל ההחלטה הזו לא התפשטה לקוד: ההעלאה
> יושבת מאחורי ממשק אחד בשם `ImageUploader` עם מימוש אחד, שנוצר ב‑`ServiceLocator`. שום ViewModel
> ושום Fragment לא יודעים ש‑Cloudinary קיימת. מעבר ל‑Storage הוא מחלקה אחת ושורה אחת.
>
> **שתיים — חוקי Firestore יכולים להגביל אילו שדות משתנים, אבל לא בכמה.** משתמש מחובר יכול
> תיאורטית לשנות את `likesCount` בערך שרירותי. אכיפה אמיתית דורשת Cloud Functions, שדורשות תוכנית
> בתשלום. זו מגבלה מודעת שאני מקבל בפרויקט קורס.
>
> **שלוש — באג שלימד אותי הכי הרבה: timeout לא מספיק כדי שכישלון offline יהיה כן.** בהתחלה
> פרסום ללא רשת נתקע לנצח, כי Firestore היא offline‑first ומקבלת כתיבה לתור מקומי. הוספתי
> `withTimeout` — וזה גרם לאפליקציה **לשקר**: היא הודיעה 'אין חיבור' ואז פרסמה את האימון בכל זאת
> כשהרשת חזרה. הפתרון הוא `NetworkGuard` שפשוט **מסרב להתחיל** כתיבה בלי חיבור מאומת.
>
> **ארבע — באג שנמצא רק בקריאת ה‑back stack.** אחרי כניסה לאימון, הניווט התחתון הפסיק לעבוד.
> `NavigationUI.setupWithNavController` מניח שהטאבים הם גרפים מקוננים, ומול הגרף השטוח שלי הוא שמר
> את מסך האימון ומיד שיחזר אותו — באותה קריאה. no‑op מושלם, בלי חריגה ובלי שום דבר בלוג.
> חיווטתי את הבר ביד, בלי `saveState` ובלי `restoreState`.
>
> **[חוזרים לאפליקציה, גוללים בפיד]**
>
> זו FitShare. תודה רבה."

---

## סיכום זמנים

| קטע | משך | מצטבר |
|---|---|---|
| מה זו האפליקציה | 0:30 | 0:30 |
| ארכיטקטורה + עץ קבצים | 1:00 | 1:30 |
| מסך אחרי מסך | 4:00 | 5:30 |
| צד השרת חי | 1:00 | 6:30 |
| ספריות | 0:45 | 7:15 |
| קשיים ומגבלות | 1:00 | 8:15 |

**אם צריך לקצץ:** מורידים את המיון ב‑2:00 ואת החלפת ערכת הנושא ב‑4:40. זה חוסך כ‑20 שניות בלי
לפגוע בשום דרישה של המחוון.

**מה שאסור לקצץ:** הטרנזקציה של הלייק (3:40), העדכון החי מהקונסולה (5:30), וקטע המגבלות (7:15).
אלה שלושת הרגעים שמראים הבנה ולא רק תפעול.
