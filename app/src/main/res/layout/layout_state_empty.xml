<?xml version="1.0" encoding="utf-8"?>
<!--
    Shared empty state. Included by every screen, never copied (SPEC section 5).
    Always carries a message and a call to action - an empty screen is an invitation to act.
    Text is set at runtime by ui/common/StateRenderer, so this file holds no screen-specific copy.
-->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="@dimen/space_xl"
    android:visibility="gone">

    <!--
        The icon is swappable, because this layout serves more than "nothing here yet": the details
        screen reuses it to say a workout was deleted, and a plus sign there invited the user to add
        something they cannot. StateRenderer sets it, defaulting to the plus.
    -->
    <ImageView
        android:id="@+id/emptyIcon"
        android:layout_width="@dimen/state_icon_size"
        android:layout_height="@dimen/state_icon_size"
        android:importantForAccessibility="no"
        android:src="@drawable/ic_empty_state" />

    <TextView
        android:id="@+id/emptyTitle"
        style="@style/TextAppearance.FitShare.Title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/space_lg"
        android:gravity="center"
        android:textAlignment="center" />

    <TextView
        android:id="@+id/emptyBody"
        style="@style/TextAppearance.FitShare.Body"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/space_sm"
        android:gravity="center"
        android:textAlignment="center" />

    <com.google.android.material.button.MaterialButton
        android:id="@+id/emptyAction"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/space_xl"
        android:minHeight="@dimen/touch_target_min"
        android:visibility="gone"
        app:shapeAppearance="@style/ShapeAppearance.FitShare.Button" />

</LinearLayout>
