<?xml version="1.0" encoding="utf-8"?>
<!-- Shown while a workout photo loads, and behind the 16:9 slot generally.
     A dedicated colour with a values-night override, so it is visible in both themes. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/placeholder_fill" />
    <corners android:radius="@dimen/radius_image" />
</shape>
