<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Firebase and Cloudinary both need the network -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Drives the "no connection" banner -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- Taking a workout photo; requested at runtime and optional (Phase 5) -->
    <uses-permission android:name="android.permission.CAMERA" />

    <!-- The app works fine on a device with no camera - gallery is always available -->
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:name=".FitShareApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:localeConfig="@xml/locales_config"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FitShare">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            Required by AppCompatDelegate.setApplicationLocales. AppCompat reaches the app context
            through this service to persist the chosen locale and, below API 33, to reapply it on
            every launch. Without it the call is silently dropped and the app falls back to the
            device language - which is exactly what happened before this was added.
            It is declared disabled on purpose: it is never started, only read as a metadata holder.
        -->
        <service
            android:name="androidx.appcompat.app.AppLocalesMetadataHolderService"
            android:enabled="false"
            android:exported="false">
            <meta-data
                android:name="autoStoreLocales"
                android:value="true" />
        </service>

        <!--
            Hands the camera app a content:// Uri to write the photo into.
            A raw file:// Uri throws FileUriExposedException from API 24 up, which is our minSdk.
        -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>
