# ARCHITECTURE_QA.md — 15 שאלות שמרצה עשוי לשאול

מסמך לימוד לקראת ההצגה בכיתה. לכל שאלה: **תשובה קצרה** למקרה שצריך לענות מיד,
**תשובה מלאה** להסבר, ו**אם ימשיכו לחפור** — ההמשך הצפוי.

**כלל אצבע:** אם אתה לא זוכר את הפרטים, תגיד את ההחלטה ואת הסיבה. מרצה מחפש שתבין **למה**,
לא שתדקלם API.

---

## 1. למה MVVM, ומה בדיוק כל שכבה עושה?

**קצר:** כדי שהלוגיקה תשרוד סיבוב מסך ותהיה ניתנת לבדיקה בלי אנדרואיד.

**מלא:**
ארבע שכבות, מלמטה למעלה:

- **`DataSource`** — השכבה **היחידה** שיודעת ש‑Firebase או Retrofit קיימים. אם מחר מחליפים מסד
  נתונים, זו השכבה היחידה שנכתבת מחדש.
- **`Repository`** — ממשק פלוס מימוש אחד. ה‑ViewModel תלוי ב**ממשק**, ולכן אפשר להחליף אותו בפייק
  בבדיקה.
- **`ViewModel`** — מחזיק את מצב המסך כ‑`LiveData<XUiState>`, ומשתמש ב‑`viewModelScope`. הוא **לא
  נוגע** ב‑`View`, ב‑`Context` ולא ב‑`Fragment`. זה מה שמאפשר לו לשרוד סיבוב מסך: ה‑Fragment נהרס
  ונבנה מחדש, ה‑ViewModel לא.
- **`Fragment`** — מנפח layout, מאזין ל‑LiveData, מצייר, ומעביר לחיצות ל‑ViewModel. אפס קריאות
  Firebase, אפס לוגיקה עסקית.

**אם ימשיכו לחפור — "למה לא MVC או MVP?"**
ב‑MVC ה‑Activity הוא גם הקונטרולר, ולכן הלוגיקה נהרסת יחד איתו בכל סיבוב מסך. ב‑MVP ה‑Presenter
מחזיק הפניה ל‑View, ולכן צריך לזכור לנתק אותו ידנית או שיש דליפה. `ViewModel` פותר את שניהם:
המערכת שומרת אותו בסיבוב, והוא לעולם לא מחזיק View.

**אם ימשיכו לחפור — "יש לכם שני מסכי פרופיל?"**
לא, **אחד**. `ProfileFragment` מקבל `userId` שיכול להיות `null`: `null` זה "הפרופיל שלי", עם עריכה,
מתג תצוגה והתנתקות; ערך אמיתי זה פרופיל של מישהו אחר, לקריאה בלבד. שני מסכים כמעט זהים שחולקים
layout הם כפילות. אותו עיקרון בדיוק בטופס ההוספה, שמשמש גם כעורך מאחורי `workoutId` שיכול להיות
`null`.

---

## 2. אין כאן Hilt. איך התלויות מוזרקות, ולמה ככה?

**קצר:** `ServiceLocator` שכתוב ביד, כי אפשר לקרוא אותו מלמעלה למטה בישיבה אחת.

**מלא:**
`di/ServiceLocator.kt` הוא אובייקט אחד שמאותחל ב‑`FitShareApp.onCreate` ומחזיק את המופעים
המשותפים — `FirebaseAuth`, `FirebaseFirestore`, ה‑Retrofit של Cloudinary — ובונה מהם את
ה‑Repositories. `di/ViewModelFactory.kt` הוא `ViewModelProvider.Factory` אחד שמקבל את התלויות
ומעביר אותן ל‑ViewModel המתאים.

הסיבה היא לא ש‑Hilt רע. הסיבה היא שעם Hilt החיווט קורה בזמן קומפילציה בקוד שנוצר אוטומטית, ואי
אפשר להצביע על שורה ולהגיד "כאן נבנה ה‑Repository". כאן אפשר.

**אם ימשיכו לחפור — "מה המחיר?"**
המחיר הוא שכל תלות חדשה דורשת שורה ב‑`ServiceLocator` ועוד ענף ב‑`ViewModelFactory`. בפרויקט עם
עשרה ViewModels זה נסבל. בפרויקט עם מאה — לא, ואז Hilt מצדיק את עצמו.

**אם ימשיכו לחפור — "זה לא Singleton גלובלי, שזה אנטי‑פטרן?"**
זה כן. ההבדל הוא שאף מחלקה **לא קוראת** ל‑`ServiceLocator` מבפנים — היא מקבלת את מה שהיא צריכה
דרך הקונסטרקטור. ה‑`ServiceLocator` נוגע רק בשתי נקודות: `FitShareApp` וה‑`Fragment` שמבקש
Factory. לכן ה‑Repositories וה‑ViewModels עצמם נשארים ניתנים לבדיקה.

---

## 3. למה ה‑Repository מחזיר `Flow` אבל ה‑Fragment רואה רק `LiveData`?

**קצר:** כי הפיד צריך לחבר זרם חי אחד עם שלושה מצבי ממשק, וזה נקי ב‑`Flow` וכאוב ב‑`LiveData`.

**מלא:**
מאזין Firestore עטוף ב‑`callbackFlow` ומוחזר כ‑`Flow<Result<T>>`. ה‑ViewModel מחבר אותו עם
הקטגוריה שנבחרה, מחרוזת החיפוש וסדר המיון — שלושה מקורות — ומסיים ב‑`.asLiveData()`.

אילו ה‑Repository היה מחזיר `LiveData`, החיבור הזה היה דורש `MediatorLiveData` **מקונן**: מדיאטור
שמאזין למדיאטור. זה עובד, אבל קשה מאוד לקרוא ולהסביר.

ולמה בכל זאת `LiveData` בסוף? כי `LiveData` מודע למחזור החיים בעצמו — הוא מפסיק להעביר ערכים
כשה‑Fragment ברקע, בלי שהמסך צריך לעשות משהו. `Flow` דורש `repeatOnLifecycle`, וזה עוד דבר שאפשר
לשכוח במסך חדש.

**אם ימשיכו לחפור — "למה `callbackFlow` ולא `flow` רגיל?"**
כי `snapshotListener` הוא callback‑based, והוא צריך להימחק כשמפסיקים להאזין. `callbackFlow` נותן
בדיוק את זה: `awaitClose { registration.remove() }`. בלי זה המאזין נשאר רשום, ממשיך לצרוך קריאות
מהמכסה, ומדליף.

**אם ימשיכו לחפור — "איך אתה מטפל באירועים חד‑פעמיים, כמו ניווט או הודעה?"**
`util/Event.kt` — עטיפה עם `getContentIfNotHandled()` שמחזירה את התוכן פעם אחת בלבד. בלי זה,
סיבוב מסך היה גורם ל‑`LiveData` להעביר את הערך האחרון שוב, וההודעה או הניווט היו קורים פעמיים.

---

## 4. איך אתם מבטיחים שמונה הלייקים לא ישקר?

**קצר:** מסמך הלייק והמונה נכתבים בתוך אותה `runTransaction`. או ששניהם נכתבים, או שאף אחד.

**מלא:**
לייק זה שתי כתיבות: יצירת `workouts/{id}/likes/{uid}`, והגדלת `likesCount` במסמך האימון. אילו הן
היו שתי פעולות נפרדות והשנייה הייתה נכשלת — למשל הרשת נופלת באמצע — היה נשאר מסמך לייק בלי שהמונה
זז, והמספר על המסך היה שקרי לנצח.

לכן שתיהן קורות בתוך `runTransaction` אחת. אותו דבר בתגובות, ואותו רעיון ב‑`WriteBatch` בפרסום
אימון, שכותב את מסמך האימון ומגדיל את `workoutsCount` יחד.

**אם ימשיכו לחפור — "בדקת את זה?"**
כן. שתים‑עשרה הקשות מהירות על הלייק, ואז השוואה: מספר מסמכי הלייק ב‑Firestore, הערך של
`likesCount`, והמספר על המסך — שלושתם זהים.

**אם ימשיכו לחפור — "מה ההבדל בין `runTransaction` ל‑`WriteBatch`?"**
`WriteBatch` הוא אטומי אבל **עיוור** — הוא לא קורא לפני שהוא כותב. `runTransaction` קורא, מחשב,
וכותב, ואם מישהו אחר שינה את המסמך בינתיים הוא **מריץ את עצמו מחדש**. לייק צריך לדעת אם המסמך כבר
קיים, ולכן טרנזקציה. פרסום אימון לא צריך לקרוא כלום, ולכן batch.

---

## 5. למה שיכפלתם את שם הכותב לתוך מסמך האימון?

**קצר:** כדי שפיד של 30 אימונים יהיה קריאה אחת ולא 31.

**מלא:**
`authorName` ו‑`authorPhotoUrl` יושבים על מסמך האימון עצמו, למרות שהם קיימים גם ב‑`users/{uid}`.
זה נקרא denormalization, וזו התאמה מכוונת למודל של Firestore: אין בו `JOIN`. בלי השכפול, כל כרטיס
בפיד היה דורש קריאה נוספת למסמך המשתמש — 30 אימונים, 31 קריאות, ומכסה חינמית שנגמרת מהר.

אותו עיקרון במועדפים: מסמך המועדף מחזיק תצלום של הכותרת, התמונה, שם הכותב והקטגוריה, ולכן כל מסך
המועדפים נטען בשאילתה **אחת**.

**אם ימשיכו לחפור — "ומה קורה כשמשתמש משנה שם?"**
זה המחיר, ואני מודע לו. אחרי עריכת פרופיל, `UserRepository` מריץ תיקון רטרואקטיבי שמעדכן את
`authorName` בכל האימונים של אותו משתמש.

**אם ימשיכו לחפור — "וגם בתגובות?"**
לא, וזו מגבלה ידועה שמתועדת ב‑README. תגובות נושאות את אותם שדות מפורקים, ותיקון מלא דורש שאילתת
collection‑group על `comments`. בפרויקט אמיתי זו הייתה Cloud Function.

---

## 6. איך אתם מונעים דליפת זיכרון עם ViewBinding בפרגמנט?

**קצר:** `_binding` מאופס ב‑`onDestroyView`, ואת זה עושה `BaseFragment` פעם אחת לכולם.

**מלא:**
ל‑`Fragment` יש שני מחזורי חיים שונים: מחזור החיים של האובייקט, ומחזור החיים של ה‑View שלו. כשמנווטים
הלאה, ה‑View נהרס אבל האובייקט שורד ב‑back stack. אם ה‑Fragment ממשיך להחזיק הפניה ל‑binding, הוא
מחזיק את כל עץ ה‑Views — וזו דליפה.

לכן `ui/common/BaseFragment.kt` מחזיק `_binding` שאפשר לאפס, חושף `binding` לא‑nullable שנגיש רק
בין `onCreateView` ל‑`onDestroyView`, ומאפס אותו ב‑`onDestroyView`. אף פרגמנט לא כותב את זה בעצמו,
ולכן אף פרגמנט לא יכול לשכוח.

**אם ימשיכו לחפור — "ואיך אתה מאזין ל‑LiveData בלי להדליף?"**
תמיד עם `viewLifecycleOwner`, לא עם `this`. ההבדל קריטי: `this` הוא מחזור החיים של ה‑Fragment,
ולכן המנוי היה שורד את הריסת ה‑View — ואחרי חזרה מה‑back stack היו **שני** מנויים מצוירים לאותו
מסך.

**אם ימשיכו לחפור — "בדקת שאין דליפה?"**
כן. שמונה מחזורי ניווט הלוך‑חזור, ואז `dumpsys meminfo`: מספר ה‑Views ירד מ‑334 ל‑132, ומספר
ה‑Activities נשאר 1.

---

## 7. מה קורה כשמסובבים את המסך?

**קצר:** ה‑ViewModel שורד, ה‑View נבנה מחדש ומצייר את אותו `UiState`.

**מלא:**
סיבוב מסך הורס את ה‑Activity ואת ה‑Fragment ובונה אותם מחדש. ה‑`ViewModel` **לא** נהרס — המערכת
מחזיקה אותו ב‑`ViewModelStore` שמשויך ל‑`NavBackStackEntry`. לכן:

- אין קריאה חוזרת ל‑Firestore, ואין קפיצה חזרה למצב טעינה
- קורוטינה שרצה באמצע (למשל העלאת תמונה) ממשיכה, כי `viewModelScope` שייך ל‑ViewModel
- ה‑Fragment החדש נרשם ל‑`LiveData` ומקבל מיד את הערך האחרון, ומצייר בדיוק את אותו מסך

**אם ימשיכו לחפור — "ומה עם מה שהמשתמש הקליד בטופס?"**
זה נשמר על ידי המערכת דרך `onSaveInstanceState` של ה‑`EditText` עצמו, וגם קיים ב‑`UiState`
של ה‑ViewModel כי כל שינוי טקסט מדווח אליו. הבדיקה שלי הייתה: להקליד חצי טופס, לסובב, ולוודא ששום
שדה לא התרוקן.

**אם ימשיכו לחפור — "מצאת באג בסיבוב?"**
כן, ומעניין: סיבוב כששדה טקסט במיקוד גרם לשדה להשתלט על כל המסך. זה מצב `extract` של המקלדת
במסך רוחב, והוא השפיע על **כל** שדה קלט באפליקציה. נפתר עם `flagNoExtractUi` על שמונה השדות.

---

## 8. למה `Result<T>` ולא פשוט לזרוק חריגות?

**קצר:** כי כישלון רשת הוא תוצאה צפויה, לא באג. הטיפוס מכריח את הקורא להתמודד איתה.

**מלא:**
כל פונקציית `suspend` בשכבת ה‑Repository וה‑DataSource מחזירה `Result<T>`. העטיפה נעשית במקום אחד,
`util/SafeCall.kt`.

היתרון הוא שהחתימה **אומרת את האמת**: אם הפונקציה מחזירה `Result<User>`, אי אפשר להשתמש בערך בלי
לטפל בכישלון. חריגה, לעומת זאת, לא מופיעה בחתימה, ולכן קל מאוד לשכוח `try/catch` — ואז האפליקציה
קורסת אצל המשתמש.

זה גם מה שמאפשר את הכלל "אין קריסות בלתי מוסברות": כל קריאת רשת עטופה, וכל כישלון עובר דרך
`ErrorMapper` להודעה בעברית.

**אם ימשיכו לחפור — "למה `ErrorMapper` מחזיר `Int` ולא `String`?"**
כי `@StringRes Int` לא דורש `Context`. זה אומר שה‑ViewModel יכול לקרוא לו — הוא לא מורשה להחזיק
`Context` — וגם שאפשר לבדוק אותו ביוניט טסט על ה‑JVM, בלי אמולטור. יש 11 בדיקות עליו.

---

## 9. איך התמודדתם עם RTL, ומה זה עלה?

**קצר:** רק `start`/`end` בכל layout, ונעילה של האפליקציה לעברית ברמת ה‑Activity.

**מלא:**
ארבעה דברים:

1. `android:supportsRtl="true"` במניפסט.
2. **בשום layout אין `left` או `right`** — רק `layout_marginStart`, `paddingEnd`,
   `layout_constraintStart_toEndOf` וכו'. זה מה שגורם לכל מסך להתהפך לבד. זו גם הסיבה
   ש‑`ConstraintLayout` כאן לא רק נוחה אלא כמעט חובה.
3. יישור טקסט עם `android:textAlignment="viewStart"`, לא `gravity="left"`.
4. אייקונים כיווניים נושאים `autoMirrored` — חץ החזרה במסך הפרטים מצביע ימינה בעברית.

והאפליקציה נעולה לעברית עם `setApplicationLocales("he")`, כדי שהיא תיראה נכון גם על מכשיר באנגלית.

**אם ימשיכו לחפור — "מה היה הקושי האמיתי?"**
באג שלקח זמן למצוא: `setApplicationLocales` הוא **no‑op שקט** כשקוראים לו מ‑`Application.onCreate`
מ‑API 33. AppCompat מעביר את הקריאה ל‑`LocaleManager` של המערכת דרך delegate של Activity פעיל, ובשלב
הזה אין כזה. בלי חריגה ובלי לוג — האפליקציה פשוט עלתה באנגלית. העברתי את הקריאה
ל‑`MainActivity.onCreate`. ניתן לשחזור רק על מכשיר שהשפה שלו אינה עברית.

**אם ימשיכו לחפור — "יש `values-en` שלא נטען אף פעם. זה לא קוד מת?"**
לא, והוא שם בכוונה ומתועד ב‑README. הוא מוכיח ששום טקסט לא מקודד קשיח ושהאפליקציה מוכנה לתרגום.

---

## 10. למה ה‑enums בלי ארגומנטים, ואיפה הם הופכים לעברית?

**קצר:** כדי ש‑`data/model/` יישאר נקי לגמרי מאנדרואיד.

**מלא:**
`WorkoutCategory` ו‑`Difficulty` הם קבועים בלבד — `{ STRENGTH, RUNNING, HIIT, ... }`. אין להם
ארגומנט ואין להם `@StringRes`.

הסיבה: אילו ה‑enum היה מחזיק `R.string.category_strength`, שכבת המודל הייתה תלויה ב‑`R` — כלומר
באנדרואיד. אז אי אפשר היה לבדוק אותה על ה‑JVM, ואי אפשר היה להעביר את המודל לשום מקום אחר.

ההמרה לעברית קורה בפונקציה **אחת** בשכבת ה‑UI, `ui/common/EnumLabels.kt`:
`@StringRes fun WorkoutCategory.labelRes(): Int`. אין מיפוי של enum לטקסט בשום מקום אחר — לא
בפרגמנט, לא באדפטר, ולא inline ב‑layout.

**אם ימשיכו לחפור — "למה `category` נשמר כמחרוזת ולא כ‑enum במודל?"**
שאלה טובה, וזו הגנה מכוונת. אילו Firestore הייתה ממירה ל‑enum בזמן ה‑deserialization, ערך שהגרסה
הזו לא מכירה — מסמך שנכתב ביד בקונסולה, או שנוצר בגרסה חדשה יותר — היה זורק חריגה ומפיל את **כל**
הפיד. כמחרוזת, `fromName` מחזיר `OTHER` והאפליקציה ממשיכה לעבוד.

**אם ימשיכו לחפור — "יש עוד מקומות כאלה?"**
כן, ובאותו דפוס: `TimeLabels.kt` הופך זמן לטקסט עברי, ו‑`NumberLabels.kt` הופך מונה לטקסט. שלושתם
בשכבת ה‑UI, ולכל אחד יש בדיוק מקום אחד.

---

## 11. למה `ListAdapter` ו‑`DiffUtil` ולא `notifyDataSetChanged`?

**קצר:** כי הפיד מגיע ממאזין חי, ולייק של מישהו אחר משדר מחדש את **כל** הרשימה.

**מלא:**
זה חשוב כאן יותר מאשר ברשימה רגילה. הפיד מגיע מ‑`snapshotListener`, ולכן כל שינוי בכל אימון —
לייק שמישהו אחר עשה, תגובה חדשה — גורם ל‑Firestore לשדר את הרשימה כולה.

עם `notifyDataSetChanged` כל כרטיס גלוי היה נקשר ומצויר מחדש בכל שינוי מרוחק, ומיקום הגלילה היה
נזרק. `DiffUtil` משווה ומצייר מחדש **רק** את מה שבאמת השתנה.

ארבעת ה‑`ItemCallback` בפרויקט משווים זהות לפי מזהה המסמך, ותוכן לפי `equals` של ה‑data class.

**אם ימשיכו לחפור — "למה `areContentsTheSame` הוא פשוט `==`?"**
כי כל המודלים הם `data class`, ולכן `equals` שלהם משווה שדה‑שדה בחינם. אילו הייתי כותב השוואה
ידנית, הוספת שדה חדש הייתה שוכחת אותו בשקט והכרטיס לא היה מתעדכן.

---

## 12. איך אתם מטפלים בהיעדר רשת, ולמה timeout לא הספיק?

**קצר:** האפליקציה מסרבת להתחיל כתיבה בלי חיבור מאומת. timeout לבדו גרם לה לשקר.

**מלא:**
זה הבאג שלימד אותי הכי הרבה בפרויקט, ושווה לספר אותו במלואו:

**שלב 1 — התסמין.** הרשמה ללא רשת הציגה גלגל טעינה לנצח. בלי שגיאה ובלי דרך לצאת.

**שלב 2 — הסיבה.** Firestore היא offline‑first. כשהשרת לא זמין היא **מקבלת** את הכתיבה לתור מקומי
ופותרת את ה‑`Task` רק כשהשרת מאשר. `await()` על ה‑`Task` הזה פשוט לא חוזר. שום דבר לא היה שבור
ב‑`ErrorMapper` — פשוט לא הייתה שגיאה למפות, כי ה‑SDK לא נכשל.

**שלב 3 — התיקון הראשון, שהיה גרוע יותר.** הוספתי `withTimeout`. התקיעה נפסקה — אבל האפליקציה
התחילה **לשקר**: היא הודיעה "אין חיבור לאינטרנט", ואז כשה‑wifi חזר, האימון פורסם בכל זאת, כי
Firestore החזיקה את ה‑batch בתור המקומי.

**שלב 4 — התיקון האמיתי.** `util/NetworkGuard.kt` בודק את `NetworkMonitor` ו**מסרב להתחיל** קריאה
כשאין חיבור מאומת. שום דבר לא נמסר ל‑SDK, ולכן שום דבר לא יכול לנחות אחר כך. ה‑timeout נשאר כרשת
ביטחון לחיבור שמת באמצע.

**אם ימשיכו לחפור — "למה לייקים ותגובות דווקא כן התנהגו נכון?"**
כי `runTransaction` **לא** מתייצרת מקומית — היא חייבת לקרוא מהשרת, ולכן היא נכשלת מיד כשאין רשת.
רק כתיבות רגילות ו‑batches מתייצרים. זה בדיוק ההבדל שהסביר למה חלק מהמסכים התנהגו נכון וחלק לא.

---

## 13. מה חוקי האבטחה שלכם **לא** יכולים לעשות?

**קצר:** הם יכולים להגביל אילו שדות משתנים, אבל לא בכמה.

**מלא:**
החוקים ב‑`firestore.rules` עושים את מה שאפשר:
- משתמש לא מחובר לא קורא כלום
- אימון נוצר רק עם `authorId` ששווה למי שכתב אותו
- עריכה ומחיקה של אימון — רק לבעלים
- `users/{uid}/favorites` — רק הבעלים, ולכן המועדפים של משתמש אחר באמת פרטיים
- מחיקת משתמש חסומה לגמרי (`allow delete: if false`)

והמגבלה: כדי שמשתמש יוכל לעשות לייק לאימון של מישהו אחר, החוק מרשה לכל משתמש מחובר לשנות את
`likesCount` ו‑`commentsCount`. ה‑`hasOnly` מגביל **אילו** שדות מותר לגעת בהם, אבל שפת החוקים לא
יכולה להגביל **בכמה** מספר משתנה. תיאורטית אפשר לשלוח `likesCount: 99999`.

אכיפה אמיתית דורשת Cloud Functions — כתיבה בצד שרת שמאמתת את הדלתא — ואלה דורשות את תוכנית Blaze
בתשלום. הפרויקט מחויב להישאר על Spark החינמית, ולכן זו מגבלה מודעת שמתועדת ב‑README.

**אם ימשיכו לחפור — "איך תדע שהחוקים באמת פורסמו?"**
שלוש בדיקות, והן כתובות ב‑README: Rules Playground בקונסולה, קריאת REST בלי טוקן שצריכה להחזיר
**403** ולא 200, ומעבר על שבע פעולות באפליקציה כדי לוודא שהחוקים לא הדוקים **מדי**.

---

## 14. למה Cloudinary ולא Firebase Storage, ואיך זה מבודד?

**קצר:** Storage דורש תוכנית בתשלום. ההעלאה יושבת מאחורי ממשק אחד, אז זו החלפה של מחלקה אחת.

**מלא:**
Firebase Storage דורש היום את תוכנית Blaze. בתוכנית Spark החינמית כל קריאה לבאקט נכשלת עם 402/403.
הפרויקט מחויב לעלות אפס וללא כרטיס אשראי, ולכן התמונות עוברות ל‑Cloudinary — שנותן העלאה
לא‑חתומה בשכבה חינמית ובלי כרטיס.

הדבר החשוב הוא שההחלטה הזו **לא התפשטה לקוד**:

```kotlin
interface ImageUploader {
    suspend fun upload(uri: Uri): Result<String>
}
```

`CloudinaryImageUploader` הוא המימוש היחיד, והוא נוצר במקום אחד — `ServiceLocator`. אף ViewModel
ואף Fragment לא יודעים ש‑Cloudinary קיימת. מעבר ל‑Storage הוא מחלקה חדשה אחת ושורה אחת.

**אם ימשיכו לחפור — "ה‑preset הזה לא סוד שדולף?"**
לא. preset לא‑חתום הוא **ציבורי מעצם הגדרתו** — הוא מוטמע בכל אפליקציית לקוח שמשתמשת בו, וכך
Cloudinary תוכננה לעבוד עם לקוחות ניידים. הוא מגיע מ‑`gradle.properties` דרך `buildConfigField`,
וזה מתועד ב‑README כדי שאף אחד לא יחשוב שנפלה טעות אבטחה.

**אם ימשיכו לחפור — "מה קורה לתמונה לפני ההעלאה?"**
נדחסת: פענוח עם `inSampleSize`, הצלע הארוכה נחתכת ל‑1080 פיקסלים, JPEG באיכות 80, הקובץ נכתב
ל‑`cacheDir` ונמחק אחרי ההעלאה בין אם הצליחה ובין אם לא. נמדד: 1600×1200 ו‑223KB יורדים ל‑1080×810
ו‑120KB. וגם `ExifInterface` מסובב לפי תג המצלמה, אחרת תמונה מצולמת עולה מסובבת ב‑90 מעלות.

---

## 15. למה חיווטתם את הניווט התחתון ביד במקום להשתמש ב‑`NavigationUI`?

**קצר:** כי `setupWithNavController` מניח שהטאבים הם גרפים מקוננים, ומול גרף שטוח הוא שובר את הבר.

**מלא:**
זה באג אמיתי שהתגלה על מכשיר: אחרי כניסה למסך אימון, הניווט התחתון הפסיק לעבוד לגמרי. הטאב היה
מסומן, ושום דבר לא קרה.

`NavigationUI.onNavDestinationSelected` מנווט כל הקשה על טאב עם:

```kotlin
popUpTo(graph.findStartDestination().id) { saveState = true }
launchSingleTop = true
restoreState = true
```

זה נכתב לאפליקציה שכל טאב שלה הוא `<navigation>` משלו. בגרף הזה `workoutDetailsFragment` הוא
**אח** של הטאבים, לא בן שלהם. `NavController.navigate` מריץ קודם את ה‑pop ואחר כך את ה‑restore,
באותה קריאה:

1. ה‑pop מוציא את מסך האימון ושומר אותו תחת המזהה שאליו עשו `popUpTo` — כלומר הפיד, שהוא יעד
   ההתחלה של הגרף.
2. שתי שורות אחר כך, `restoreState` מחפש מצב שמור תחת המזהה שאליו מנווטים — ובשביל טאב "פיד" זה
   **אותו מזהה** שנכתב בשלב 1.

כלומר ההקשה שומרת את מסך האימון ומיד מחזירה אותו. ה‑stack יוצא זהה, ה‑helper מדווח שההקשה לא
טופלה, והמסך לא משתנה. בלי חריגה, ובלי שום דבר בלוג.

התיקון: לחווט את הבר ביד ב‑`MainActivity`, בלי `saveState` ובלי `restoreState`. הקשה על טאב תמיד
מגיעה לשורש הטאב.

**אם ימשיכו לחפור — "למה לא פשוט לקנן את הטאבים בגרפים?"**
כי זה היה **משמר** את ההתנהגות השנייה שדווחה כבאג: יציאה מטאב וחזרה אליו היו מחזירות אותי למסך
האימון שהייתי בו, במקום לרשימה. וגם כי `workoutDetailsFragment` נגיש מהפיד, מהמועדפים ומהפרופיל,
ולכן קינון היה דורש לשכפל אותו לשלושה תת‑גרפים.

**אם ימשיכו לחפור — "איך אתה יודע שזו הייתה הסיבה ולא ניחוש?"**
כי מדדתי. הוספתי מאזין זמני שהדפיס את ה‑back stack **לפני** ההקשה ו**אחרי**, וראיתי שהוא יוצא
זהה בדיוק, ושה‑helper מחזיר `false`. אחרי התיקון חזרתי על אותה בדיקה משלושה מסכי אימון שנפתחו
משלושה טאבים שונים.

---

## שלוש שאלות שכדאי להיות מוכן אליהן, בלי תשובה מוכנה

1. **"תראה לי איפה בקוד קורה X."** תרגל ניווט מהיר ב‑Android Studio: `Ctrl+N` לשם מחלקה,
   `Ctrl+Shift+F` לטקסט. אל תגלול בעץ הקבצים מול הכיתה.
2. **"מה היית עושה אחרת?"** תשובה כנה: Cloud Functions לאכיפת המונים, תיקון שמות הכותבים גם
   בתגובות, ובדיקות רבות יותר על שכבת ה‑Repository מול פייק.
3. **"כמה מזה כתבת בעצמך?"** תענה בכנות, ותהיה מוכן להסביר **כל** שורה שתצביע עליה. זה בדיוק מה
   שהמסמך הזה נועד לו.
